[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 -m uvicorn api.server:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
